spring:
  datasource:
    url: "jdbc:h2:mem:csh-test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH"
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: false
  flyway:
    enabled: false
  sql:
    init:
      mode: never

logging:
  level:
    root: WARN
    com.cookai.csh: INFO
